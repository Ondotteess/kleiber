// Package main is a tiny fixture program used by Kleiber's project-model
// integration tests. It is intentionally minimal: one package, one file,
// one function, so tests can assert exactly what the loader reports.
package main

import "fmt"

func main() {
	fmt.Println("hello, kleiber")
}
