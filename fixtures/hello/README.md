# fixtures/hello

Tiny single-module fixture used by `internal/project`'s integration tests.

- One Go module: `github.com/Ondotteess/kleiber/fixtures/hello`
- One package: `main`
- One file: `main.go`

This fixture is intentionally minimal so tests can make exact assertions
about what the loader reports. Do not "improve" it.
